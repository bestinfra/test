import React, { createContext, useContext, useEffect, useState } from 'react';import {  AUTH_CONFIG,  getStoredToken,  setStoredToken,  getStoredUser,  setStoredUser,  clearAuthData,} from '../../config/auth';export interface User {  id: number;  username: string;  email: string;  firstName: string;  lastName: string;  role: 'admin' | 'moderator' | 'accountant' | 'ADMIN' | 'MODERATOR' | 'ACCOUNTANT';  accessLevel?: string;  locationId?: number;  roles?: string[];}export interface LoginResult {  success: boolean;  message?: string;}export interface AuthContextType {  user: User | null;  token: string | null;  isAuthenticated: boolean;  login: (identifier: string, password: string, appId?: string) => Promise<LoginResult>;  logout: () => void;  loading: boolean;  updateAuthState: () => void;}const AuthContext = createContext<AuthContextType | undefined>(undefined);export const useAuth = () => {  const context = useContext(AuthContext);  if (context === undefined) {    throw new Error('useAuth must be used within an AuthProvider');  }  return context;};interface AuthProviderProps {  children: React.ReactNode;}export const AuthProvider: React.FC<AuthProviderProps> = ({ children }) => {  const [user, setUser] = useState<User | null>(null);  const [token, setToken] = useState<string | null>(null);  const [loading, setLoading] = useState(true);  // Check for existing token on mount  useEffect(() => {    const storedToken = getStoredToken();    const storedUser = getStoredUser();    if (storedToken && storedUser) {      try {        setToken(storedToken);        setUser(storedUser);      } catch (error) {        console.error('Error parsing stored user data:', error);        clearAuthData();      }    }    setLoading(false);  }, []);  const login = async (    identifier: string,    password: string,    appId?: string  ): Promise<LoginResult> => {    try {      // Sub-apps use the application-backend authentication      const endpoint = '/sub-app/auth/login';      const requestBody = { identifier, password, appId: appId || AUTH_CONFIG.APP_ID };      console.log('Login attempt:', { endpoint, requestBody });      const response = await fetch(endpoint, {        method: 'POST',        headers: {          'Content-Type': 'application/json',        },        body: JSON.stringify(requestBody),      });      console.log('Response status:', response.status);      if (!response.ok) {        console.error('HTTP Error:', response.status, response.statusText);        return { success: false, message: `HTTP ${response.status}: ${response.statusText}` };      }      const responseText = await response.text();      console.log('Response text:', responseText);      let data;      try {        data = JSON.parse(responseText);      } catch (parseError) {        console.error('JSON parse error:', parseError);        return { success: false, message: 'Invalid response from server' };      }      if (data.success && data.data) {        const { user: userData, token: userToken } = data.data;        // Store in localStorage using unique keys        setStoredToken(userToken);        setStoredUser(userData);        // Update state        setToken(userToken);        setUser(userData);        return { success: true };      } else {        console.error('Login failed:', data.message);        return { success: false, message: data.message || 'Login failed' };      }    } catch (error) {      console.error('Login error:', error);      return { success: false, message: 'Network error occurred' };    }  };  const logout = () => {    clearAuthData();    setToken(null);    setUser(null);  };  const updateAuthState = () => {    console.log('LocalAuthWrapper: updateAuthState called');    const storedToken = getStoredToken();    const storedUser = getStoredUser();    console.log('LocalAuthWrapper: storedToken:', storedToken ? 'exists' : 'null');    console.log('LocalAuthWrapper: storedUser:', storedUser ? 'exists' : 'null');    if (storedToken && storedUser) {      try {        console.log('LocalAuthWrapper: parsed userData:', storedUser);        setToken(storedToken);        setUser(storedUser);        console.log('LocalAuthWrapper: State updated - token and user set');      } catch (error) {        console.error('LocalAuthWrapper: Error parsing stored user data:', error);        clearAuthData();        setToken(null);        setUser(null);      }    } else {      console.log('LocalAuthWrapper: No stored auth data, clearing state');      setToken(null);      setUser(null);    }  };  const value: AuthContextType = {    user,    token,    isAuthenticated: !!token && !!user,    login,    logout,    loading,    updateAuthState,  };  return <AuthContext.Provider value={value}>{children}</AuthContext.Provider>;};// Default export for lazy loadingexport default AuthProvider;